type ProfileProps = {
  name: string;
  homeworld: string;
  species: string;
};

type ProfileRowProps = {
  fieldName: string;
  fieldValue: string;
};

export type { ProfileProps, ProfileRowProps };
